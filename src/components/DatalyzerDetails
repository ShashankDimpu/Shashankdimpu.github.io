import React from 'react';

const DatalyzerDetails = ({ navigateTo }) => {
  return (
    <div>
      <h2>Datalyzer - Software Engineer</h2>
      <p>
        Spearheaded the design and implementation of scalable RESTful APIs in C#, facilitating robust data exchange mechanisms that served as the backbone for front-end functionalities.
      </p>
      <button onClick={() => navigateTo('services')}>Back to Services</button>
    </div>
  );
};

export default DatalyzerDetails;
